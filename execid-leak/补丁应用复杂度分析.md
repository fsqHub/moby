# v1.13.1 源码合入 patches 的复杂度分析

## 概览

**总 patch 数**：12 个  
**时间跨度**：2017-01 至 2023-06（6.5 年）  
**版本跨度**：v17.04.0-ce 至 v25.0.0（8 个主要版本）

---

## 复杂度评估总结

| Patch 组 | 补丁数 | 复杂度 | 可应用性 | 推荐 |
|---------|-------|--------|---------|------|
| **Group 1** (原始修复) | 1 | ⭐⭐ 低 | ✅ 高 | 优先 |
| **Group 2** (Stream 重构) | 2 | ⭐⭐⭐⭐⭐ 极高 | ❌ 低 | 跳过 |
| **Group 3** (I/O 处理) | 2 | ⭐⭐⭐⭐ 高 | ⚠️ 中 | 选择性 |
| **Group 4** (Monitor 超时) | 1 | ⭐⭐⭐ 中 | ⚠️ 中 | 选择性 |
| **Group 5** (Context Cancel) | 2 | ⭐⭐⭐⭐ 高 | ❌ 低 | 困难 |
| **Group 6** (Client Disconnect) | 2 | ⭐⭐⭐⭐⭐ 极高 | ❌ 极低 | 不推荐 |
| **Group 7** (Race 修复) | 1 | ⭐⭐⭐ 中 | ⚠️ 中 | 选择性 |

---

## 详细分析

### Group 1: 原始问题修复（2017-01）

#### Patch: `0001-Fix-303111-dockerd-leaks-ExecIds-on-failed-exec-i.patch`

**复杂度**: ⭐⭐ 低

**修改文件**:
```
daemon/exec.go                  | +7 -0
api/server/httputils/errors.go  | 修改错误处理
```

**修改内容**:
```go
// daemon/exec.go - ContainerExecStart()
if err != nil {
+   // 新增：启动失败时删除 ExecConfig
+   c.ExecCommands.Delete(ec.ID)
    return err
}
```

**可应用性**: ✅ **高（95%）**

**原因**:
- ✅ 修改量小（< 10 行）
- ✅ 逻辑简单（添加 cleanup）
- ✅ 文件结构未变
- ✅ API 兼容

**潜在冲突**:
- ⚠️ 错误处理逻辑可能略有不同
- ⚠️ ExecCommands 结构可能需要微调

**应用难度**: **低**

**预估时间**: 30 分钟

**推荐**: ✅ **优先应用**

---

### Group 2: Stream 重构与竞态修复（2017-01～02）

#### Patch 1: `0001-Move-attach-code-to-stream-package.patch`

**复杂度**: ⭐⭐⭐⭐⭐ 极高

**修改文件**:
```
container/container.go          | 大量删除
container/stream/attach.go      | 新增文件（~500 行）
container/stream/streams.go     | 新增文件
daemon/attach.go                | 大量重构
api/server/router/.../attach.go | 接口变更
```

**修改内容**:
- 创建全新的 `container/stream` 包
- 将 attach 逻辑从 daemon 迁移到 stream
- 重构整个 attach 调用链

**可应用性**: ❌ **极低（10%）**

**原因**:
- ❌ 架构级重构
- ❌ 新增包和文件
- ❌ 大量接口变更
- ❌ 调用链完全重写

**潜在冲突**:
- ❌ v1.13.1 没有 `container/stream` 包
- ❌ daemon/attach.go 结构完全不同
- ❌ 需要同步修改几十个调用点

**应用难度**: **极高**

**预估时间**: 8-16 小时（手动移植）

**推荐**: ❌ **跳过（代价远大于收益）**

---

#### Patch 2: `0001-Resolve-race-conditions-in-attach-API-call.patch`

**复杂度**: ⭐⭐⭐⭐ 高

**修改文件**:
```
container/stream/attach.go      | 依赖 Patch 1
container/container.go          | 锁机制调整
```

**可应用性**: ❌ **无（依赖 Patch 1）**

**推荐**: ❌ **跳过**

---

### Group 3: Runtime 与 I/O 处理改进（2019-06）

#### Patch 1: `0001-Handle-blocked-I-O-of-exec-d-processes.patch`

**复杂度**: ⭐⭐⭐⭐ 高

**修改文件**:
```
container/stream/streams.go     | 依赖 stream 包
daemon/monitor.go               | context 超时
container/container.go          | Wait() 签名变更
```

**修改内容**:
```go
// 关键修改：为 Wait() 添加 context
-func (c *Config) Wait() error
+func (c *Config) Wait(ctx context.Context) error

// monitor.go - 使用 timeout
+ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)
+defer cancel()
+err := stream.Wait(ctx)
```

**可应用性**: ⚠️ **中等（40%）**

**原因**:
- ⚠️ 依赖 `container/stream` 包（不存在）
- ✅ 核心思想可移植（添加 timeout）
- ⚠️ 需要大量 API 适配

**手动移植可能性**: ⚠️ **可能**

**核心移植点**:
```go
// v1.13.1 的 daemon/monitor.go
// 手动添加超时逻辑
func (d *Daemon) handleContainerExit(...) {
    // 原有逻辑
-   container.WaitStop(-1 * time.Second)
    
    // 新增超时
+   done := make(chan struct{})
+   go func() {
+       container.WaitStop(-1 * time.Second)
+       close(done)
+   }()
+   
+   select {
+   case <-done:
+       // 正常完成
+   case <-time.After(30 * time.Second):
+       log.Warn("container cleanup timeout")
+   }
}
```

**应用难度**: **高（需要手动适配）**

**预估时间**: 4-6 小时

**推荐**: ⚠️ **选择性应用（理解核心思想，手动实现）**

---

#### Patch 2: `0001-Send-exec-exit-event-on-failures.patch`

**复杂度**: ⭐⭐ 低

**修改文件**:
```
daemon/monitor.go               | +5 行
```

**修改内容**:
```go
// 失败路径也发送 exit event
if err != nil {
+   attributes := map[string]string{"exitCode": "1"}
+   daemon.LogContainerEventWithAttributes(container, "exec_die", attributes)
    return err
}
```

**可应用性**: ✅ **高（80%）**

**原因**:
- ✅ 修改量小
- ✅ 逻辑独立
- ⚠️ event 系统可能略有不同

**应用难度**: **低**

**预估时间**: 30 分钟

**推荐**: ✅ **推荐应用**

---

### Group 4: Monitor 超时控制（2020-11）

#### Patch: `0001-handleContainerExit-put-a-timeout-on-containerd-Dele.patch`

**复杂度**: ⭐⭐⭐ 中

**修改文件**:
```
daemon/monitor.go               | +10 行
```

**修改内容**:
```go
// 为 containerd DeleteTask 添加超时
-err := ctr.DeleteTask(ctx)
+ctx, cancel := context.WithTimeout(ctx, 10*time.Second)
+defer cancel()
+err := ctr.DeleteTask(ctx)
```

**可应用性**: ⚠️ **中等（50%）**

**原因**:
- ✅ 逻辑简单
- ⚠️ v1.13.1 使用 containerd 0.2.x（API 不同）
- ⚠️ DeleteTask 方法签名可能不同

**手动移植**: ⚠️ **需要适配**

**核心思想**:
```go
// v1.13.1 可能的实现
// 需要查看实际 containerd API
timeout := make(chan bool, 1)
go func() {
    time.Sleep(10 * time.Second)
    timeout <- true
}()

done := make(chan bool, 1)
go func() {
    err = ctr.Delete()
    done <- true
}()

select {
case <-done:
    // 正常完成
case <-timeout:
    log.Warn("DeleteTask timeout")
}
```

**应用难度**: **中**

**预估时间**: 2-3 小时

**推荐**: ⚠️ **选择性应用（需要熟悉 containerd 0.2.x API）**

---

### Group 5: Context Cancel 机制（2022-08）

#### Patch 1: `0001-daemon-kill-exec-process-on-ctx-cancel.patch`

**复杂度**: ⭐⭐⭐⭐ 高

**修改文件**:
```
daemon/exec.go                  | +30 行
```

**修改内容**:
```go
// ContainerExecStart - 监听 context cancel
+go func() {
+    select {
+    case <-ctx.Done():
+        // Kill exec process
+        if err := daemon.containerd.SignalProcess(cid, ec.ID, syscall.SIGKILL); err != nil {
+            log.Error(err)
+        }
+    case <-attachErr:
+        // exec 正常完成
+    }
+}()
```

**可应用性**: ⚠️ **中等（30%）**

**原因**:
- ✅ 核心逻辑清晰
- ⚠️ containerd API 版本差异
- ⚠️ context 传递链路可能不同
- ⚠️ SignalProcess 方法可能不存在

**手动移植**: ⚠️ **需要大量适配**

**应用难度**: **高**

**预估时间**: 6-8 小时

**推荐**: ⚠️ **困难（需要深入理解 v1.13.1 的 exec 实现）**

---

#### Patch 2: `0001-daemon-Maintain-container-exec-inspect-invariant.patch`

**复杂度**: ⭐⭐⭐ 中

**修改文件**:
```
daemon/exec.go                  | 调整删除顺序
```

**修改内容**:
```go
// 维护 ExecCommands 不变量：先删除，后清理 Running
-c.ExecCommands.Set(ec.ID, nil)  // 标记为 nil
-c.ExecCommands.Delete(ec.ID)    // 删除
+c.ExecCommands.Delete(ec.ID)    // 先删除
+ec.Running = false              // 后清理
```

**可应用性**: ✅ **高（70%）**

**原因**:
- ✅ 逻辑简单
- ✅ 文件存在
- ⚠️ ExecCommands 实现可能不同

**应用难度**: **中**

**预估时间**: 1-2 小时

**推荐**: ✅ **推荐应用**

---

### Group 6: Client Disconnect 检测（2023-02）

#### Patch 1: `0001-Fix-goroutine-fd-leak-when-client-disconnects.patch`

**复杂度**: ⭐⭐⭐⭐⭐ 极高

**修改文件**:
```
api/server/router/container/notify_linux.go  | 新增文件（epoll 实现）
api/server/router/container/container_routes.go | 接口变更
daemon/attach.go                              | 大量修改
```

**修改内容**:
- 新增 epoll 监听 client fd
- 实现 disconnect 检测机制
- 重构 attach API 调用链

**可应用性**: ❌ **极低（5%）**

**原因**:
- ❌ 新增复杂文件（epoll syscall）
- ❌ API router 结构差异大
- ❌ 需要理解 HTTP hijack 机制
- ❌ 涉及底层网络编程

**应用难度**: **极高**

**预估时间**: 16-24 小时

**推荐**: ❌ **不推荐（代价极高）**

---

#### Patch 2: `0001-Fix-fd-leak-goroutine-when-attaching-stdin-only.patch`

**复杂度**: ⭐⭐⭐⭐ 高

**修改文件**:
```
container/stream/attach.go      | 依赖 stream 包
```

**可应用性**: ❌ **无（依赖不存在的包）**

**推荐**: ❌ **跳过**

---

### Group 7: Race Condition 修复（2023-06）

#### Patch: `0001-daemon-fix-panic-on-failed-exec-start.patch`

**复杂度**: ⭐⭐⭐ 中

**修改文件**:
```
daemon/monitor.go               | +10 行
```

**修改内容**:
```go
// 处理 execConfig.Process == nil 的 race
func (daemon *Daemon) ProcessEvent(...) {
+   if ec.Process == nil {
+       return errors.New("process is nil")
+   }
    exitCode := ec.Process.ExitCode()
}
```

**可应用性**: ✅ **高（70%）**

**原因**:
- ✅ 逻辑简单（nil 检查）
- ✅ 文件存在
- ⚠️ Process 结构可能不同

**应用难度**: **低**

**预估时间**: 30 分钟

**推荐**: ✅ **推荐应用**

---

## 总体策略建议

### 最小修复集（推荐）

**目标**：修复核心问题，最小改动

**应用 patches**：
1. ✅ Group 1: 原始修复（3cc0d6bb04）
2. ✅ Group 3-2: 失败路径 event（c08d4da6e5）
3. ✅ Group 5-2: ExecCommands 不变量（a09f8dbe6e）
4. ✅ Group 7: Race 修复（3b28a24e97）

**预估工作量**：4-6 小时

**预期效果**：修复 `invalidcommand` 场景（约 30%）

---

### 中等修复集（平衡）

**目标**：修复主要问题，适度改动

**应用 patches**：
- 最小集 +
- ⚠️ Group 3-1: I/O timeout（手动移植核心思想）
- ⚠️ Group 4: Monitor 超时（手动移植）

**预估工作量**：12-16 小时

**预期效果**：改善拓展场景（约 50-60%）

---

### 完整修复（不推荐）

**目标**：应用所有 patches

**问题**：
- ❌ Group 2（Stream 重构）→ 架构级改动，16+ 小时
- ❌ Group 5-1（Context Cancel）→ 需要深入理解，8+ 小时
- ❌ Group 6（Client Disconnect）→ 极度复杂，24+ 小时

**预估工作量**：60-80 小时

**预期效果**：接近完整修复（约 80-90%）

**推荐**: ❌ **不推荐（直接升级到 v24.0+ 更合理）**

---

## 复杂度矩阵

| Patch | 修改行数 | 文件数 | 新增文件 | API 变更 | 架构影响 | 总复杂度 |
|-------|---------|-------|---------|---------|---------|---------|
| Group 1 | < 10 | 2 | 0 | 无 | 无 | ⭐⭐ |
| Group 2-1 | 500+ | 10+ | 3+ | 重大 | 重构 | ⭐⭐⭐⭐⭐ |
| Group 2-2 | 100+ | 3 | 0 | 中等 | 中等 | ⭐⭐⭐⭐ |
| Group 3-1 | 150+ | 5 | 0 | 中等 | 中等 | ⭐⭐⭐⭐ |
| Group 3-2 | < 10 | 1 | 0 | 无 | 无 | ⭐⭐ |
| Group 4 | 15 | 1 | 0 | 小 | 无 | ⭐⭐⭐ |
| Group 5-1 | 50+ | 1 | 0 | 中等 | 小 | ⭐⭐⭐⭐ |
| Group 5-2 | 20 | 1 | 0 | 无 | 无 | ⭐⭐⭐ |
| Group 6-1 | 300+ | 5 | 2 | 重大 | 中等 | ⭐⭐⭐⭐⭐ |
| Group 6-2 | 50+ | 1 | 1 | 中等 | 小 | ⭐⭐⭐⭐ |
| Group 7 | 15 | 1 | 0 | 无 | 无 | ⭐⭐⭐ |

---

## 关键障碍

### 1. container/stream 包不存在

**影响 patches**：
- Group 2（全部）
- Group 3-1
- Group 6-2

**解决方案**：
- 跳过 Group 2
- 手动移植核心逻辑到现有文件

---

### 2. containerd API 版本差异

**v1.13.1**: containerd 0.2.x  
**patches**: 为 containerd 1.x+ 设计

**影响 patches**：
- Group 3-1
- Group 4
- Group 5-1

**解决方案**：
- 查阅 containerd 0.2.x API 文档
- 实现等效功能

---

### 3. Context 传递链路

v1.13.1 的 context 使用不如新版本完善

**影响 patches**：
- Group 3-1
- Group 5-1

**解决方案**：
- 手动构造 context
- 使用 channel + timeout 模拟

---

## 最终建议

### 选项 A：最小修复集 + 升级

```
应用 4 个简单 patches（4-6 小时）
    ↓
验证效果（修复约 30%）
    ↓
升级到 v24.0.0+（完整修复）
```

**推荐度**: ⭐⭐⭐⭐⭐

---

### 选项 B：手动移植核心思想

```
理解 patches 的核心修复逻辑
    ↓
手动实现等效功能（16-24 小时）
    ↓
验证效果（修复约 60%）
```

**推荐度**: ⭐⭐⭐（仅适合学习）

---

### 选项 C：完全应用所有 patches

```
逐个适配所有 patches（60-80 小时）
    ↓
大量测试和调试
    ↓
效果不确定（可能 80-90%）
```

**推荐度**: ⭐（不推荐，代价远大于收益）

---

## 工作量评估

| 方案 | 时间 | 修复程度 | 风险 | 推荐 |
|------|------|---------|------|------|
| **最小集** | 4-6 小时 | 30% | 低 | ⭐⭐⭐⭐⭐ |
| **中等集** | 12-16 小时 | 50-60% | 中 | ⭐⭐⭐ |
| **完整集** | 60-80 小时 | 80-90% | 高 | ⭐ |
| **直接升级** | 2-4 小时 | 100% | 低 | ⭐⭐⭐⭐⭐ |

**结论**：除非有特殊原因，**直接升级到 v24.0.0 或 v26.1.4 是最佳选择**。
